{
  "name": "arsene-weekly-ranking-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler types worker-configuration.d.ts && tsc --noEmit",
    "test": "vitest run",
    "types": "wrangler types worker-configuration.d.ts",
    "dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "latest",
    "typescript": "latest",
    "vitest": "latest",
    "wrangler": "latest"
  }
}
